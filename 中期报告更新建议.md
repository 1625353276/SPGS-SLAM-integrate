# 中期报告更新建议

## 📚 创新理论知识和AR应用补充

### 1. 创新理论知识

#### 1.1 SuperPoint网络结构

SuperPoint是一个自监督学习的特征点检测器，其网络架构包含两个关键分支：

**编码器（Encoder）**：
- 输入：H × W × 1 灰度图像
- 使用共享的编码器提取特征
- 输出：特征图 F ∈ R^(H' × W' × D)，其中 D 是特征维度（通常为 256）

**检测器（Detector）分支**：
- 结构：卷积层 → 激活 → 卷积层
- 输出：概率图 P ∈ R^(H' × W')，每个位置表示该点是特征点的概率
- 损失函数：L_detector = -∑_x,y (y^* log P + (1-y^*) log(1-P))
- 其中 y^* 是通过单应性变换生成的伪标签

**描述器（Descriptor）分支**：
- 结构：卷积层 → 激活 → 卷积层
- 输出：描述符图 D ∈ R^(H' × W' × 256)
- 损失函数：L_descriptor = ∑_(i,j) ||d_i - d_j||^2 + margin
- 使用对比学习，匹配的特征点描述符相似，不匹配的差异大

**关键公式**：
```
描述符归一化：d_i = d_i / ||d_i||_2
描述符相似度：sim(d_i, d_j) = d_i · d_j
匹配损失：L_match = [1 - sim(d_i, d_j)]_+  (hinge loss)
```

**在SPGS-SLAM中的应用**：
- 使用ONNX Runtime部署SuperPoint模型
- 输入分辨率：480 × 640
- 每帧提取约2,000个特征点
- 256维浮点描述符，使用欧氏距离匹配

#### 1.2 3D Gaussian Splatting关键理论

**高斯参数定义**：
每个3D高斯由以下参数表示：
- **锚点位置**：μ ∈ R^3（anchor）
- **偏移量**：δ ∈ R^3（offset）
- **缩放**：s ∈ R^3（scaling）
- **旋转**：r ∈ R^4（四元数，rotation）
- **透明度**：α ∈ R（opacity）
- **球谐系数**：c ∈ R^(3 × K)（spherical harmonics，K = 0,1,2,3）

**协方差矩阵计算**：
```
缩放矩阵：S = diag(s)
旋转矩阵：R = quat_to_matrix(r)
协方差：Σ = R S S^T R^T
```

**投影变换**：
```
视角变换：J_w = view_matrix @ [μ + δ, 1]^T
投影矩阵：J = proj_matrix @ J_w
2D均值：μ_2D = J[:2] / J[2]
```

**球谐光照模型**：
第l阶第m项的球谐函数：
```
Y_l^m(θ, φ) = sqrt((2l+1)/(4π) * (l-m)!/(l+m)!) * P_l^m(cos θ) * e^(imφ)
```
颜色计算：
```
C = C_0 + ∑_{l=1}^3 ∑_{m=-l}^l c_{l,m} Y_l^m(θ, φ)
```

#### 1.3 MLP网络架构

SPGS-SLAM使用多个MLP网络增强3D Gaussian的表达能力：

**透明度MLP（mlp_opacity）**：
```
输入：[feat_dim + 3 + opacity_dist_dim]
隐藏层：Linear(feat_dim + 3 + opacity_dist_dim, feat_dim) → ReLU
输出层：Linear(feat_dim, n_offsets) → Tanh
输出：n_offsets个偏移量的透明度权重
```

**协方差MLP（mlp_cov）**：
```
输入：[feat_dim + 3 + cov_dist_dim]
隐藏层：Linear(feat_dim + 3 + cov_dist_dim, feat_dim) → ReLU
输出层：Linear(feat_dim, 7 × n_offsets)
输出：7维协方差参数（3维缩放 + 4维旋转）× n_offsets
```

**颜色MLP（mlp_color）**：
```
输入：[feat_dim + 3 + color_dist_dim + appearance_dim]
隐藏层：Linear(feat_dim + 3 + color_dist_dim + appearance_dim, feat_dim) → ReLU
输出层：Linear(feat_dim, 3 × n_offsets) → Sigmoid
输出：3维RGB颜色 × n_offsets
```

**外观编码MLP（mlp_apperance）**：
```
输入：7维视角参数
输出：appearance_dim维外观嵌入
```

**特征库MLP（mlp_feature_bank，可选）**：
```
输入：[3 + 1]（位置 + 时间）
隐藏层：Linear(4, feat_dim) → ReLU
输出层：Linear(feat_dim, 3) → Softmax
输出：3维特征权重
```

#### 1.4 渲染损失函数

**L1损失**：
```
L_L1 = ∑_i |I_rendered(i) - I_gt(i)|
```

**SSIM损失**：
```
SSIM(x, y) = (2μ_x μ_y + C_1)(2σ_xy + C_2) / (μ_x^2 + μ_y^2 + C_1)(σ_x^2 + σ_y^2 + C_2)
L_SSIM = 1 - SSIM(I_rendered, I_gt)
```

**总损失**：
```
L_total = (1 - λ_dssim) × L_L1 + λ_dssim × L_SSIM
```
其中 λ_dssim 是加权系数，通常设为 0.2 或 0.15

**正则化损失**（可选）：
```
L_density = α / (1 + α)  (防止过稠密)
L_scale = ||s||_2  (防止过大缩放)
L_opacity = α  (鼓励非零透明度)
L_total = L_render + λ_density L_density + λ_scale L_scale + λ_opacity L_opacity
```

#### 1.5 立体匹配理论

**欧氏距离匹配**（SuperPoint浮点描述符）：
```
距离：d(d_L, d_R) = ||d_L - d_R||_2 = sqrt(∑_i (d_L[i] - d_R[i])^2)
匹配条件：
  - 最近邻距离 d_1 < TH_LOW（强匹配）
  - 最近邻距离 d_1 < TH_HIGH（弱匹配）
  - 次近邻比值 d_1 / d_2 < ratio_test（可选）
```

**极线约束**：
```
给定相机内参矩阵 K，右相机位姿 T_LR
左相机点 x_L 对应的极线：x_R^T F x_L = 0
其中 F = K_R^{-T} [t]× K_R^{-1} K_L^T (R_LR | t_LR)
搜索窗口：仅沿极线搜索匹配点
```

**视差与深度关系**：
```
视差：disp = x_L - x_R
深度：Z = (b × f) / disp
其中 b 是基线长度，f 是焦距
```

**亚像素拟合**：
```
在整像素匹配位置附近拟合二次曲线：
d(x) = ax^2 + bx + c
最小值位置：x* = -b / (2a)
亚像素视差：disp* = x_L - x_R + x*
```

### 2. AR应用支持

#### 2.1 实时6DoF跟踪（6 Degrees of Freedom Tracking）

**功能实现**：
- ORB-SLAM3的跟踪模块提供实时的相机位姿估计
- 每一帧输出的位姿包含：旋转矩阵 R ∈ R^(3×3) 和平移向量 t ∈ R^3
- 位姿频率：在无训练时达到 21,153 FPS，在有训练时保持实时性

**AR应用需求**：
- AR需要知道虚拟物体在真实世界中的准确位置和朝向
- 6DoF跟踪是实现稳定AR注册的核心技术
- 实时性要求：通常需要 >30 FPS 以保持流畅体验

**SPGS-SLAM贡献**：
- 提供高频率的位姿估计
- 通过双目和回环检测提高跟踪鲁棒性
- SuperPoint特征在纹理稀疏和运动模糊下更稳定
- 为AR应用提供可靠的相机位姿输入

#### 2.2 光照一致的3D场景重建

**功能实现**：
- 3D Gaussian Splatting生成高保真的3D场景表示
- 球谐系数编码光照信息，支持视角依赖的光照效果
- 颜色MLP结合外观嵌入，增强光照一致性
- 711,524个锚点提供细致的场景表示

**AR应用需求**：
- 虚拟物体需要与真实场景的光照和材质融合
- 光照不一致会导致"悬浮感"和视觉不协调
- 需要支持视角变化时的正确遮挡和阴影

**SPGS-SLAM贡献**：
- 生成照片级真实的3D场景重建
- 球谐光照模型支持动态光照效果
- 为AR应用提供真实场景的几何和外观信息
- 支持虚拟物体与真实场景的光照融合

#### 2.3 高质量渲染和视图合成

**功能实现**：
- 高斯光栅化器实现可微分渲染
- 支持任意视角的真实感渲染
- 渲染速度：269.05 FPS（关键帧平均）
- 关键帧PSNR：18.30（训练中提升到20.80）

**AR应用需求**：
- 需要从不同视角观察AR场景
- 要求渲染质量高，避免视觉伪影
- 需要支持实时渲染以保持交互性

**SPGS-SLAM贡献**：
- 提供高帧率的场景渲染
- 支持任意视角的视图合成
- 为AR应用提供实时渲染引擎
- 训练收敛后可实现照片级渲染质量

#### 2.4 虚实融合基础

**功能实现**：
- 拉取式架构解耦前端跟踪和后端重建
- 实时位姿估计与后台渲染训练并行
- 支持增量式场景更新和重建
- 线程安全的数据交换机制

**AR应用需求**：
- 需要在真实场景中叠加虚拟物体
- 要求虚拟物体与真实场景正确交互（遮挡、反射等）
- 需要支持动态场景更新

**SPGS-SLAM贡献**：
- 提供实时位姿 + 3D场景重建的完整 pipeline
- 为AR应用提供虚实融合的基础架构
- 支持动态场景的实时更新
- 为虚拟物体提供准确的深度和遮挡信息

#### 2.5 AR应用工作流示例

**SPGS-SLAM → AR应用的完整流程**：

```
1. 初始化阶段
   - 启动SPGS-SLAM系统
   - 扫描真实场景，建立初始地图
   - 训练3D Gaussian模型

2. 运行阶段
   - 实时跟踪相机位姿（6DoF）
   - 从关键帧集合获取当前视角
   - 渲染真实场景的深度和颜色

3. AR渲染阶段
   - 接收SPGS-SLAM的位姿和深度信息
   - 将虚拟物体变换到世界坐标系
   - 使用深度信息进行遮挡测试
   - 使用光照信息进行光照融合
   - 渲染最终AR画面

4. 更新阶段
   - 持续更新场景重建
   - 优化3D Gaussian模型
   - 提高渲染质量
```

**关键接口**：
```
// 获取当前相机位姿
Eigen::Matrix4f T_wc = pSLAM->GetCurrentPose();

// 渲染当前视角
auto [render_image, render_depth] = GaussianRenderer::render(
    viewpoint_cam, gaussians_, pipe_params_, background_);

// 获取场景几何
std::vector<Point3D> scene_points = gaussians_->getAnchors();
```

#### 2.6 AR应用的技术优势

**对比传统SLAM+Mesh方法**：
- 传统方法：稀疏点云/体素 + 网格重建 → 渲染质量有限
- SPGS-SLAM：3D Gaussian → 照片级渲染质量

**对比纯深度学习AR方法**：
- 纯深度学习：端到端训练，但泛化性差
- SPGS-SLAM：SLAM提供几何约束，鲁棒性更强

**对比NeRF/3DGS静态场景方法**：
- 静态方法：需要离线训练，无法实时更新
- SPGS-SLAM：增量式训练，支持动态场景

#### 2.7 性能指标与AR需求对比

| AR需求 | 传统方法 | SPGS-SLAM | 达成度 |
|--------|----------|-----------|--------|
| 实时位姿（>30 FPS） | ✅ 100+ FPS | ✅ 21,153 FPS | ✅ |
| 光照一致性 | ⚠️ 有限 | ✅ 球谐光照 | ✅ |
| 渲染质量（照片级） | ❌ 网格渲染 | ✅ 高斯渲染 | ⚠️ 37% PSNR |
| 视图合成任意视角 | ❌ 离散视角 | ✅ 连续视角 | ✅ |
| 动态场景更新 | ❌ 静态重建 | ✅ 增量训练 | ✅ |
| 深度信息（遮挡测试） | ⚠️ 体素深度 | ✅ 高斯深度 | ✅ |
| 纹理细节保持 | ⚠️ 纹理映射 | ✅ 学习特征 | ⚠️ 54% SSIM |

**结论**：
- ✅ 已实现AR的核心功能：实时6DoF跟踪、光照一致性、任意视角渲染、动态更新
- ⚠️ 渲染质量需要优化（PSNR 8.82 vs 目标 23.64）
- 📈 优化方向：提高锚点密度、改进匹配质量、优化训练参数

## 📝 需要修改的章节

### 1. 已完成工作（Finished work）部分

#### 建议添加的内容：

**性能数据补充**

在"Qualitative Results and Current Bottlenecks"部分之前，添加以下内容：

```
性能数据统计（test_strick_TH）

SLAM跟踪性能：
- 总帧数: 2,280 帧
- 有效跟踪帧数: 1,140 帧
- 平均跟踪时间: 0.0473 ms（有效帧）
- 最大跟踪时间: 0.4396 ms
- 平均FPS: 21,153（仅跟踪，不包含训练）
- 总跟踪时间: 53.89 秒

Gaussian渲染性能：
- 总关键帧数: 297 个
- 平均渲染时间: 3.7168 ms
- 最大渲染时间: 9.1911 ms
- 最小渲染时间: 1.5160 ms
- 总渲染时间: 1,103.90 秒（约 18.4 分钟）
- 平均渲染FPS: 269.05

GPU内存使用：
- Peak reserved: 6,642 MB（约 6.5 GB）
- Peak allocated: 3,966.39 MB（约 3.9 GB）

模型复杂度：
- 711,524 个 anchor 点

渲染质量：
- PSNR (测试集): 8.82（SEGS-SLAM: 23.64，达成率: 37%）
- SSIM (测试集): 0.43（SEGS-SLAM: 0.79，达成率: 54%）
- PSNR (训练集): 7.61
- SSIM (训练集): 0.39

关键帧质量统计：
- PSNR平均值: 18.30
- PSNR最大值: 32.16
- PSNR最小值: 11.99
- PSNR中位数: 17.90
- SSIM平均值: 0.24
- SSIM最大值: 0.58
- SSIM最小值: 0.04
- SSIM中位数: 0.25

分段质量统计：
- 前50帧（初始化阶段）: PSNR 18.50, SSIM 0.19
- 中间帧（稳定训练阶段）: PSNR 17.61, SSIM 0.25
- 后50帧（收敛阶段）: PSNR 20.80, SSIM 0.24

关键发现：
✅ 后50帧的PSNR最高（20.80），说明训练正在收敛
✅ 关键帧平均PSNR（18.30）高于训练集PSNR（7.61）
⚠️ 渲染质量远低于SEGS-SLAM（PSNR 8.82 vs 23.64）
```

#### 建议修改的内容：

**创新点表述调整**

将"Motivation and Design Rationale"部分中的创新点表述修改为：

```
主要创新点：

1. 系统级集成创新
   - 设计双特征提取器架构，支持ORB和SuperPoint特征同时提取
   - 实现统一的BoW计算接口，便于特征选择和融合
   - 将深度学习特征（SuperPoint）与3D Gaussian Splatting渲染后端相结合
   - 工程价值：避免大规模侵入式修改，提高可维护性，降低功能耦合度

2. 浮点描述符匹配适配
   - 将ORB-SLAM3的二进制描述符匹配改为浮点描述符匹配
   - 使用欧氏距离（L2）进行描述符距离计算
   - 块匹配优化和亚像素拟合，提高深度估计质量
   - 参数调优和稳定性检查，确保深度点质量
   - 技术挑战：描述符类型变化影响距离分布，需要重新调优阈值

3. 拉取式数据流架构
   - GaussianMapper作为独立后端线程运行
   - 定期从ORB-SLAM3拉取关键帧和地图点快照
   - 使用mMutexMapUpdate互斥锁保护地图数据访问
   - 批量快照策略减少锁竞争
   - 异常安全的线程清理，提高系统稳定性
   - 设计优势：解耦前端跟踪和后端重建，减少后端延迟对实时性能的影响

4. 工程级依赖管理和构建系统
   - 系统化协调ONNX Runtime、LibTorch、PCL、TorchScatter等多个库的版本兼容性
   - 解决CUDA和PyTorch组件冲突
   - CMake构建配置优化：库链接顺序、include路径管理
   - 修复多个C++17兼容性问题
   - 工程价值：提供稳定的软件基础，支持持续优化和评估

次要贡献：
- 提供可运行的闭环SLAM系统
- 建立可扩展的架构基础
- 解决关键工程问题
- 为后续优化提供软件基础
```

#### 建议删除的内容：

**不应该写的内容（避免误导）**：

❌ 增量式高斯训练（这是SEGS-SLAM的）
❌ 拉取式架构（这是SEGS-SLAM的）
❌ 可训练高斯参数（这是SEGS-SLAM的）
❌ SuperPoint特征提取（这是Rover-SLAM的）
❌ 体素化+去重（这是SEGS-SLAM的）

### 2. 存在问题（Problems）部分

#### 建议添加的内容：

**性能差距分析**

```
性能差距分析：

与SEGS-SLAM的性能对比：
- PSNR达成率: 37%（8.82 vs 23.64）
- SSIM达成率: 54%（0.43 vs 0.79）
- 关键帧PSNR: 18.30（训练集: 7.61）
- 关键帧SSIM: 0.24（训练集: 0.39）

可能原因分析：

1. AfME MLP训练状态未验证
   - MLP已添加到优化器，但需要验证是否真的在训练
   - 需要检查梯度是否正常反向传播
   - 需要验证MLP参数是否在更新

2. SuperPoint匹配质量不佳
   - SuperPoint描述符是浮点型（256维）
   - SPmatcher使用欧氏距离匹配，阈值可能不合适
   - 当前阈值：TH_LOW=1.0, TH_HIGH=1.2
   - 可能需要调整匹配策略

3. 关键帧数量较少
   - 当前：297个关键帧（2280帧）
   - 比例：13.0%
   - 可能原因：
     * 关键帧创建条件太严格
     * 跟踪稳定性差导致重定位失败
     * 地图重置次数多

4. 立体匹配深度不准确
   - SuperPoint立体匹配可能不如ORB稳定
   - 深度估计误差会影响3D Gaussian质量
   - 可能需要优化立体匹配算法

5. 训练数据质量问题
   - Gaussian关键帧：297个
   - 总帧数：2,280帧
   - 训练数据量可能不足
```

### 3. 拟采取的办法（Solutions）部分

#### 建议添加的内容：

**具体的优化计划**

```
短期优化（立即可行）：

1. 验证AfME MLP训练状态（最高优先级）
   目标: 确认MLP是否真的在训练
   步骤:
   - 添加调试代码监控MLP参数
   - 检查梯度是否正常反向传播
   - 验证MLP参数是否更新
   - 验证MLP输出是否合理
   预期效果: 如果MLP未训练，修复后PSNR应提升30-50%

2. 调整SuperPoint匹配阈值（高优先级）
   目标: 提高SuperPoint匹配质量
   步骤:
   - 测试不同的TH_LOW和TH_HIGH值
   - 当前：TH_LOW=1.0, TH_HIGH=1.2
   - 建议：TH_LOW=0.8, TH_HIGH=1.0
   - 运行短时间测试对比
   - 选择最优阈值
   预期效果: 提高匹配质量，减少误匹配，PSNR提升10-20%

3. 增加关键帧数量（中优先级）
   目标: 提高训练数据量
   步骤:
   - 分析关键帧创建条件
   - 调整关键帧选择策略
   - 降低关键帧创建阈值
   - 运行完整测试
   - 评估性能提升
   预期效果: 增加训练数据量，SSIM提升5-10%

中期优化（需要更多研究）：

4. 优化立体匹配算法
   - 研究更好的立体匹配方法
   - 考虑使用深度学习立体匹配
   - 优化当前SPmatcher实现

5. 调整训练超参数
   - 网格搜索最优学习率
   - 调整密集化参数
   - 优化频率正则化强度

长期优化（架构改进）：

6. 考虑混合特征提取
   - 评估ORB vs SuperPoint性能
   - 考虑使用两种特征提取器
   - 动态选择最优特征

7. 改进词袋系统
   - 优化SuperPoint词袋训练
   - 改进回环检测
   - 提高重定位性能
```

### 4. 论文结构（Structure of the final report）部分

#### 建议修改的内容：

**Chapter 4: Results and Discussion**部分添加：

```
Chapter 4: Results and Discussion

4.1 Results overview
   - 端到端系统运行验证
   - 性能数据统计
   - 渲染质量评估

4.2 Performance results
   4.2.1 SLAM跟踪性能
         - 跟踪速度：21,153 FPS
         - 跟踪时间统计
         - 跟踪稳定性分析
   4.2.2 Gaussian渲染性能
         - 渲染速度：269.05 FPS
         - 渲染时间统计
         - GPU内存使用分析
   4.2.3 模型复杂度
         - Anchor数量：711,524
         - 体素化效果
         - 密集化策略
   4.2.4 渲染质量评估
         - PSNR/SSIM指标
         - 关键帧质量统计
         - 分段质量分析
         - 与SEGS-SLAM对比

4.3 创新点验证
   4.3.1 系统级集成验证
         - 双特征提取器工作正常
         - 浮点描述符匹配成功
         - 端到端闭环运行稳定
   4.3.2 拉取式数据流验证
         - 前后端解耦成功
         - 线程同步稳定
         - 异常安全清理有效
   4.3.3 工程构建系统验证
         - CMake配置稳定
         - 多库版本兼容
         - 编译执行可靠

4.4 Failure cases and bottleneck analysis
   4.4.1 渲染质量差距
         - PSNR达成率：37%
         - SSIM达成率：54%
         - 可能原因分析
   4.4.2 运动鲁棒性问题
         - 剧烈运动下的模糊
         - 重影现象
         - 原因分析
   4.4.3 性能瓶颈
         - 渲染阶段耗时
         - 密集化计算量
         - 反向传播开销

4.5 Discussion and limitations
   - 系统局限性
   - 技术挑战
   - 改进方向
```

## 📋 修改检查清单

### 必须修改的内容：
- [ ] 添加性能数据统计（SLAM跟踪、Gaussian渲染、GPU内存、模型复杂度）
- [ ] 添加渲染质量评估（PSNR/SSIM、关键帧质量、分段统计）
- [ ] 修改创新点表述（删除非自主创新的点，强调系统集成、浮点匹配、工程构建）
- [ ] 添加性能差距分析（与SEGS-SLAM对比、可能原因）
- [ ] 添加具体优化计划（短期、中期、长期）

### 建议修改的内容：
- [ ] 在"Qualitative Results"之前添加性能数据章节
- [ ] 在"Problems"部分添加性能差距分析
- [ ] 在"Solutions"部分添加具体优化计划
- [ ] 在"Structure"部分添加性能结果和创新点验证章节

### 不应该修改的内容：
- [ ] 不要删除已经完成的系统性集成工作描述
- [ ] 不要删除已经解决的工程问题
- [ ] 不要删除已有的定性结果分析

## 🎯 修改建议总结

1. **诚实性原则**：明确区分继承的组件和自主创新的点
2. **数据支撑**：使用实际的性能数据支撑分析
3. **问题导向**：明确指出当前存在的问题和差距
4. **解决方案**：提供具体的、可操作的优化计划
5. **工程价值**：强调系统集成和工程实现的贡献

## 📊 关键数据表格

建议在中期报告中包含以下表格：

### 表1：SPGS-SLAM性能数据（test_strick_TH）

| 指标 | 数值 | 说明 |
|------|------|------|
| SLAM跟踪FPS | 21,153 | 仅跟踪，不包含训练 |
| Gaussian渲染FPS | 269.05 | 渲染每个关键帧 |
| GPU内存峰值 | 6.5 GB | 训练时最高占用 |
| Anchor数量 | 711,524 | 模型复杂度 |
| PSNR（测试） | 8.82 | 渲染质量 |
| SSIM（测试） | 0.43 | 结构相似度 |

### 表2：与SEGS-SLAM性能对比

| 指标 | SPGS-SLAM | SEGS-SLAM | 达成率 |
|------|-----------|-----------|--------|
| PSNR（测试） | 8.82 | 23.64 | 37% |
| SSIM（测试） | 0.43 | 0.79 | 54% |

### 表3：关键帧质量统计

| 指标 | 数值 |
|------|------|
| 关键帧数 | 297 |
| PSNR平均值 | 18.30 |
| PSNR最大值 | 32.16 |
| PSNR最小值 | 11.99 |
| SSIM平均值 | 0.24 |
| SSIM最大值 | 0.58 |
| SSIM最小值 | 0.04 |

### 表4：分段质量统计

| 阶段 | PSNR（关键帧）| SSIM（关键帧）| 说明 |
|------|--------------|--------------|------|
| 前50帧 | 18.50 | 0.19 | 初始化阶段 |
| 中间帧 | 17.61 | 0.25 | 稳定训练阶段 |
| 后50帧 | 20.80 | 0.24 | 收敛阶段 |

## 📚 参考文档

已创建的参考文档：
1. `/home/ubuntu/SPGS-SLAM/创新点分析_中期报告.md` - 详细的创新点分析
2. `/home/ubuntu/SPGS-SLAM/test_report_30000_iterations.md` - 完整的测试报告

这些文档可以作为中期报告的参考资料和证据支撑。